pull_request_rules:
  - name: bot-approval-probe
    conditions:
      - base=main
      - "#approved-reviews-by>=1"
    actions:
      comment:
        message: "BOTAPPROVAL-PROBE-553 mergify counted an approval"
